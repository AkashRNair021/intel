CONNECT;

CREATE TABLE STUDENTT(
    STUDENTID NUMBER(4) PRIMARY KEY,
    STUDENTNAME VARCHAR2(50),
    AGE NUMBER(2),
    DEPARTMENT VARCHAR2(49)
);

DESC STUDENTT;

ALTER TABLE STUDENTT ADD EMAIL VARCHAR2(67);

DESC STUDENTT;

RENAME STUDENTT TO STUDENTT1;

TRUNCATE TABLE STUDENTT1;

DROP TABLE STUDENTT1;
